namespace Wanes.Shareds.Enums;

/// <summary>
/// Section a FAQ entry is filed under. The clients group the help screen by
/// this, so the set stays short enough to scan on a phone.
/// </summary>
public enum FaqCategory
{
    General = 1,
    Riding = 2,
    Driving = 3,
    Account = 4,
    Safety = 5,
}
